version: '3.8'

services:
  db:
    image: postgres
    restart: always
    ports:
      - 5433:5432
    networks:
        - purplenet
    volumes:
      - "./postgres-data:/var/lib/postgresql/data"

volumes:
  postgres-data:

networks:
  purplenet:
